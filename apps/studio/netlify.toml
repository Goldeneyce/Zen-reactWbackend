[build]
  command = "npx sanity build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA routing: serve index.html for all paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
